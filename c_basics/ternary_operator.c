//ternary_operator

#include <stdio.h>

int main(){
    //ternary_operator is a short hand form of if else statement
    // here it's syntax '
    // (condition ) ? value_if_its_true : value_if_its_false ;

    int a= 10;

    printf("%s \n", (a > 5) ? "greater \n" : "less \n" );
    return 0;

}
